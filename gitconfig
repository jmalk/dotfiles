# This is Git's per-user configuration file.
[user]
  name = Josh Malkinson
  email = josh.malkinson@lush.co.uk
[alias]
  co = checkout
  br = branch
  ci = commit
  a = add
  d = diff
  ds = diff --staged
  s = status
[pull]
  rebase = true
[fetch]
  prune = true
[filter "lfs"]
  clean = git-lfs clean -- %f
  smudge = git-lfs smudge -- %f
  process = git-lfs filter-process
  required = true
